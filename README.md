# Email_Spam_Detection_ML_Classifier
In this paper, Two Machine Learning Models are used namely Naive Bayes classifier and Logistic Regression to classify messages as spam or ham.

Logistic Regression works better than Bayes Classfier on training set while accurary on test set is approximately equal(logistic regression leads)
